docker-compose down
docker-compose down --volumes --remove-orphans
docker network prune -f
docker-compose up -d
--------------------------
docker-compose down --volumes --remove-orphans
docker rmi $(docker images -q)
docker-compose up --build -d

-------------------------


 
docker-compose down
docker network prune
docker rm $(docker ps -aq)
docker-compose up -d





---------------


Dans ton cas, les noms des conteneurs sont définis dans le fichier docker-compose.yml. Voici comment entrer dans chacun de tes conteneurs :

Firewall :

bash
Copier
Modifier
docker exec -it firewall bash
Router :

bash
Copier
Modifier
docker exec -it router bash
DNS :

bash
Copier
Modifier
docker exec -it dns bash
DHCP :

bash
Copier
Modifier
docker exec -it dhcp bash
Web Server :

bash
Copier
Modifier
docker exec -it web_server bash
Squid Proxy :

bash
Copier
Modifier
docker exec -it squid bash
Syslog :

bash
Copier
Modifier
docker exec -it syslog bash
Workstation :

bash
Copier
Modifier
docker exec -it workstation bash



docker logs workstation

docker-compose down
docker-compose up -d
